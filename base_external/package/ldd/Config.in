config BR2_PACKAGE_LDD
        bool "ldd"
        help
          Includes the modules included in misc-modules and scull folder of ldd3 respository in the kernel image.
          Used to understand buildroot external trees and package builds.

